[project]
name = "persona-clone"
version = "0.1.0"
description = "Text-to-text persona/style replication pipeline: data acquisition, RAG, and fine-tuning."
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
